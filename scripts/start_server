#!/bin/bash

# Enable AllowOverride for .htaccess files in /var/www/html
cat > /etc/httpd/conf.d/allowoverride.conf << 'EOF'
<Directory "/var/www/html">
    AllowOverride All
    Options FollowSymLinks
</Directory>
EOF

# Restart Apache to apply changes
service httpd restart
